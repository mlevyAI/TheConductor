# macOS
.DS_Store
.AppleDouble
.LSOverride

# Conductor runtime state (generated per-project, not part of the agent)
.conductor/

# Claude Code local settings
.claude/settings.local.json

# Environment files
.env
.env.local
.env.*

# Editor
.vscode/
.idea/
*.swp
*.swo

# Agent-monitor runtime output (logged session activity + generated reports).
# Never commit these — they capture bash commands, agent prompts, and file
# paths that may include secrets pasted on the command line.
agent-monitor/activity.jsonl
agent-monitor/reports/
